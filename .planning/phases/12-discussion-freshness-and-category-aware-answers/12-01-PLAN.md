---
phase: 12-discussion-freshness-and-category-aware-answers
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/discussions/analyze.ts
  - src/services/discussions/filters.ts
  - src/services/discussions/request-intent.ts
  - tests/unit/services/discussions.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Current/fresh/latest status questions and explicit date or time scoped discussion questions trigger a Forge-managed refresh without the user asking for force sync."
    - "Summary, pattern, and gap-analysis questions about already-fetched local discussion context continue to use cached artifacts when no explicit freshness trigger is present."
    - "Refresh decisioning is derived from one normalized request-intent path instead of scattered analyzer heuristics."
  artifacts:
    - path: src/services/discussions/request-intent.ts
      provides: normalized analyzer intent parsing for scope, refresh decision, and parsed filters
      min_lines: 100
    - path: src/services/discussions/analyze.ts
      provides: orchestration that consumes normalized intent for implicit refresh behavior
      min_lines: 160
    - path: tests/unit/services/discussions.test.ts
      provides: regression coverage for freshness-triggered refresh and cached local behavior
      min_lines: 260
  key_links:
    - from: src/services/discussions/request-intent.ts
      to: src/services/discussions/analyze.ts
      via: normalized refresh decision and parsed question scope
      pattern: refreshMode
    - from: src/services/discussions/request-intent.ts
      to: src/services/discussions/filters.ts
      via: question-derived temporal filters normalized through shared filter utilities
      pattern: normalizeDiscussionFilters
---

<objective>
Add a dedicated request-intent layer that parses analyzer questions and drives implicit refresh orchestration.

Purpose: Phase 12 only works if freshness is determined from user intent rather than a manual `refresh` boolean.
Output: Analyzer orchestration that refreshes automatically for current-status and explicit time-scoped requests while preserving cached-local behavior for summary and gap-analysis questions.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-discussion-freshness-and-category-aware-answers/12-CONTEXT.md
@.planning/phases/12-discussion-freshness-and-category-aware-answers/12-RESEARCH.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-01-PLAN.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-02-PLAN.md
@.planning/phases/11-runtime-self-sufficiency-and-agent-discipline/11-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract normalized discussion request intent</name>
  <files>src/services/discussions/request-intent.ts, src/services/discussions/filters.ts, src/services/discussions/analyze.ts</files>
  <action>Create a dedicated request-intent helper that accepts the analyzer question plus explicit CLI options and returns normalized scope, parsed temporal filters, answer-shape hints, and refresh decision metadata. Support explicit current-status wording plus the explicit date and window forms already called out in research, including `today`, `yesterday`, `last week`, `since`, `after`, `before`, `between ... and ...`, and explicit date or datetime phrases that clearly scope discussions by time. Reuse shared filter normalization instead of duplicating date-boundary logic inside `analyze.ts`.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The analyzer has one authoritative parsing path for question scope and freshness intent.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire analyzer orchestration around implicit refresh intent</name>
  <files>src/services/discussions/analyze.ts, src/services/discussions/request-intent.ts</files>
  <action>Refactor `runDiscussionAnalyzer()` so it resolves request intent before loading or refreshing data, then uses that intent to choose between refresh and cached digest paths. Preserve explicit CLI refresh overrides, refresh automatically for current-status requests and explicit time-scoped discussion questions, and continue using local prepared artifacts when the request is a summary, theme, or gap-analysis query that is not freshness-sensitive. Keep the failure mode clear when required artifacts are missing and the request still cannot fetch. Do not invent or rely on a periodic background sync schedule, because current Forge has none.</action>
  <verify>
    <automated>npm test -- discussions</automated>
  </verify>
  <done>Freshness-sensitive questions no longer depend on the user saying `force sync`, while cached local questions still stay local.</done>
</task>

<task type="auto">
  <name>Task 3: Add parser and refresh-decision regression coverage</name>
  <files>tests/unit/services/discussions.test.ts</files>
  <action>Add unit coverage for normalized intent parsing and refresh decisioning. Include positive cases for explicit relative windows, date ranges, and current-status wording, negative cases that should not trigger refresh on generic summary/theme/gap-analysis prompts alone, explicit refresh flag precedence, and the cached-local path for summary or pattern questions without a time scope.</action>
  <verify>
    <automated>npm test -- discussions</automated>
  </verify>
  <done>The phase has automated evidence that implicit refresh is driven by explicit time scope rather than unstable heuristics.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the extracted request-intent flow
- [ ] Current-status and explicit time-scoped questions trigger refresh without a manual refresh flag
- [ ] Non-time-scoped local questions continue using cached prepared artifacts
- [ ] `npm test -- discussions` covers parser and refresh-decision regressions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Analyzer orchestration is driven by normalized request intent instead of a raw boolean
- Current-status and explicit time-scoped requests refresh automatically while cached-local questions remain fast and local
  </success_criteria>

<output>
After completion, create `.planning/phases/12-discussion-freshness-and-category-aware-answers/12-01-SUMMARY.md`
</output>
