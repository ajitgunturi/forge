---
phase: 12-discussion-freshness-and-category-aware-answers
plan: "02"
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/contracts/discussions.ts
  - src/services/discussions/analyze.ts
  - src/services/discussions/prepare.ts
  - tests/unit/services/discussions.test.ts
autonomous: true
requirements:
  - EXTD-03
user_setup: []
must_haves:
  truths:
    - "Analyzer answers group results by the repository's actual GitHub discussion categories rather than a flat mixed summary."
    - "Derived kind and status insights remain visible inside each category section instead of replacing the real category grouping."
    - "Issue requests receive a terse discussions-only redirect that preserves the requested period when possible."
  artifacts:
    - path: src/services/discussions/analyze.ts
      provides: category-first answer rendering and terse discussions-only redirect behavior
      min_lines: 220
    - path: src/services/discussions/prepare.ts
      provides: prepared digest data needed for category-grouped summaries and per-category breakdowns
      min_lines: 120
    - path: tests/unit/services/discussions.test.ts
      provides: regression coverage for category rendering and short issue redirection
      min_lines: 320
  key_links:
    - from: src/services/discussions/prepare.ts
      to: src/services/discussions/analyze.ts
      via: prepared category data consumed by category-first rendering
      pattern: category
    - from: src/contracts/discussions.ts
      to: src/services/discussions/prepare.ts
      via: prepared digest shape for category-aware output
      pattern: PreparedDiscussionDigest
---

<objective>
Refactor discussion answers to be category-first and replace the verbose issues correction with the required terse redirect.

Purpose: Phase 12 is not complete if answers still flatten everything into one mixed view or respond to issues requests with a long corrective essay.
Output: Analyzer output that navigates by real GitHub discussion categories and a short discussions-only redirect for issue-scoped questions.
</objective>

<execution_context>
@/Users/ajitg/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ajitg/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-discussion-freshness-and-category-aware-answers/12-CONTEXT.md
@.planning/phases/12-discussion-freshness-and-category-aware-answers/12-RESEARCH.md
@.planning/phases/12-discussion-freshness-and-category-aware-answers/12-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare digest structures for category-first rendering</name>
  <files>src/contracts/discussions.ts, src/services/discussions/prepare.ts</files>
  <action>Extend the prepared discussion data model only as needed to support category-grouped rendering cleanly. Preserve the real GitHub category identity in the prepared records, decide deliberately whether category slug or per-category totals belong in the digest, and keep the existing derived `kind` and `status` signals available for intra-category summaries.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The prepared digest contains the category information needed to render grouped answers without re-deriving unstable state from raw text at render time.</done>
</task>

<task type="auto">
  <name>Task 2: Replace flat rendering with category-first answers and terse issue redirect</name>
  <files>src/services/discussions/analyze.ts</files>
  <action>Refactor answer rendering so the primary navigation is by actual GitHub category. Keep global metadata and count summaries, add a category summary section, then render repeated category sections with counts, status and kind breakdowns, and matching discussion details under each real category. Remove the existing mislabeled `Category` output that currently shows `kind`, and replace the long `issues` correction block with a one- or two-line discussions-only redirect that offers the equivalent discussions request for the same requested period when one was parsed.</action>
  <verify>
    <automated>npm test -- discussions</automated>
  </verify>
  <done>Answers are discussion-native, category-aware, and terse when the user asks for issues.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for category grouping and redirection</name>
  <files>tests/unit/services/discussions.test.ts</files>
  <action>Add unit tests that prove output is grouped by actual GitHub category labels, that derived kind and status still appear inside category sections, that count summaries continue working after the renderer rewrite, and that issue-scoped questions now receive the short redirect rather than the previous multi-paragraph guidance.</action>
  <verify>
    <automated>npm test -- discussions</automated>
  </verify>
  <done>The category-first renderer and terse redirect are protected against regression.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds with the category-aware renderer changes
- [ ] Analyzer answers are grouped by real GitHub categories present in the matching records
- [ ] Derived status and kind insights remain visible within category sections
- [ ] Issue-scoped requests receive the short discussions-only redirect
- [ ] `npm test -- discussions` covers grouped rendering and redirect behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Category labels in analyzer output correspond to real GitHub discussion categories instead of derived kinds
- The analyzer redirects issues questions tersely and helpfully
  </success_criteria>

<output>
After completion, create `.planning/phases/12-discussion-freshness-and-category-aware-answers/12-02-SUMMARY.md`
</output>
